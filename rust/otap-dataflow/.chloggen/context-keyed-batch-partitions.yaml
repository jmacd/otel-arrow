change_type: enhancement
component: pipeline
note: "Allow batch processing to partition by one primitive or composite context entry."
issues: [3924]
subtext: "Each key accumulates independently within a configured active-partition limit. Incomplete entries share an empty-context partition; outputs retain only the selected key fields."
