change_type: enhancement
component: pipeline
note: "Pdata context values now use versioned compiled registers, and header propagation supports explicit output names."
issues: [3872]
subtext: |
  Consumers reject compiler versions without configured symbols, and Kafka topic routing rejects registers with multiple values. Set `output_name` to emit a fixed header name without retaining input-name provenance.
